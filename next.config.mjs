/** @type {import('next').NextConfig} */
const nextConfig = {
    // basePath: "/2048-in-react", // base dir name
    // output: "export",  // <=== enables static exports
    // reactStrictMode: true,
};

export default nextConfig;
